name: ALPapp
description: Client for interfacing with the ALP LMS

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dropdown_formfield: ^0.1.3
  firebase_auth: ^0.18.1+2
  firebase_core: 0.5.0+1
  flutter_barcode_scanner: ^1.0.1
  flutter_signin_button: ^1.0.0
  provider: ^4.3.2+2
  splashscreen: 1.2.0
  sqflite: ^1.3.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.15.4


flutter:
  uses-material-design: true
 